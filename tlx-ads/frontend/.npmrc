# TLX-ADS Frontend
# Este projeto NÃO usa npm workspaces.
# Isso evita que uma configuração global do usuário (ex.: ~/.npmrc com workspaces=true)
# quebre `npm install` aqui dentro.
workspaces=false
include-workspace-root=false

# Garante que ferramentas (ex.: RHDA) não tentem rodar npm em modo workspaces.
# Alguns ambientes têm ~/.npmrc com workspaces=true e isso quebra "npm install --package-lock-only".
workspaces=false
include-workspace-root=false
